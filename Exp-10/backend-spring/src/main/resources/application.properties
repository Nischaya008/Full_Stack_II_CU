# Server
server.port=5000

# MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/codehive?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=123456789
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Jackson
spring.jackson.serialization.write-dates-as-timestamps=false

# JWT
jwt.secret=CodeHiveNG_Super_Secret_JWT_Key_2026_Spring_Boot
jwt.expiration=3600000

# Pusher (set these as environment variables or replace with your values)
pusher.appId=1933365
pusher.key=1933365
pusher.secret=b20a99aea1918571eab8
pusher.cluster=ap2
